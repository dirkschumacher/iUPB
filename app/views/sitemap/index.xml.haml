- base_url = "http://#{request.host_with_port}"
!!! XML
%urlset{:xmlns => "http://www.sitemaps.org/schemas/sitemap/0.9"}
  - for post in @courses
    %url
      %loc #{base_url}#{course.paul_id}
      %changefreq monthly
      %priority 0.5