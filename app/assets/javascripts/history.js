// Prepare
/*window.iUPB.History = window.History; // Note: We are using a capital H instead of a lower h
if ( !window.iUPB.History.enabled ) {
  // History.js is disabled for this browser.
  // This is because we can optionally choose to support HTML4 browsers or not.
  return false;
}*/